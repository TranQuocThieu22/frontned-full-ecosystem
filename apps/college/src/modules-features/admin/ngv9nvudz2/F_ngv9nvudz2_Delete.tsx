'use client'
import MyActionIconDelete from "@/components/ActionIcons/ActionIconCRUD/MyActionIconDelete";

export default function F_ngv9nvudz2_Delete({ id, contextData }: { id: number, contextData: string }) {
    return (
        <MyActionIconDelete title="Xác nhận xóa dữ liệu?" contextData={contextData} onSubmit={()=>{}}></MyActionIconDelete>
    )
}