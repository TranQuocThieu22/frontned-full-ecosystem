'use client';

import { MyActionIconDelete } from "aq-fe-framework/components";

export default function F_cgbkl8o4nr_Delete({ id }: { id: string }) {
    return (
        <>
            <MyActionIconDelete onSubmit={() => { }} contextData={id} />
        </>
    );
}