'use client'

import { MyActionIconDelete } from "aq-fe-framework/components"

export default function F_u8zfwd46p2_Delete({ id, agentCode }: { id: number, agentCode: string }) {

    return <MyActionIconDelete
        contextData={agentCode}
        onSubmit={() => { }} >
    </MyActionIconDelete>
}

