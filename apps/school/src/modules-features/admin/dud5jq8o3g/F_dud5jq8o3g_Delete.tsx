'use client'

import { MyActionIconDelete } from "aq-fe-framework/components"

export default function F_dud5jq8o3g_Delete({ id, contextData }: { id: number, contextData: string }) {
    return <MyActionIconDelete contextData={contextData} onSubmit={() => { }}></MyActionIconDelete>
}

