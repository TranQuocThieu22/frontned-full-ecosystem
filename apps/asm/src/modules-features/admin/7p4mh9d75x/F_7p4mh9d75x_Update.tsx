import useS_7p4mh9d75x from './S_7p4mh9d75x';

export default function F_7p4mh9d75x_Update() {
    const store = useS_7p4mh9d75x()
    return "s"
    // const mutation = useMutation({
    //     mutationFn: async () => {
    //         const menu = store.getAllSubMenus().map(item => {
    //             const { name, ...rest } = item;
    //             return { ...rest };
    //         })
    //         return await baseAxios.put('/Role/UpdatePermission', {
    //             RoleId: store.state.userId,
    //             pagePermissions: menu
    //         });
    //     }
    // })
    // return (
    //     <MyButton crudType='save' w={'100%'} onClick={() => mutation.mutate()} />
    // )
}
